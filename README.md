# Storm_Events

## Dataset

The dataset (`StormEvents.7z`, ~1.35GB uncompressed) is too large to store in the repo directly. Download it from the [data-v1 release](https://github.com/claudio1975/Storm_Events/releases/tag/data-v1) and extract it into the `data/` folder.